node_modules
# dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Mobile build output (regenerated via `npm run mobile:build`)
dist-mobile/

# Capacitor native folders are generated by `npm run mobile:add:ios` /
# `npm run mobile:add:android`. After you've configured signing & icons,
# commit them. Until then they're ignored.
ios/
android/
